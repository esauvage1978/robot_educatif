/** Clé localStorage pour le choix cookies (mesure d'audience GA4). */
export const COOKIE_CONSENT_STORAGE_KEY = 'robot-educatif-cookie-consent';

export type CookieConsentValue = 'accepted' | 'rejected';
